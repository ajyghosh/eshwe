const test=require('node:test');const assert=require('node:assert/strict');const {loadSource}=require('./helpers/source.cjs');
test('receipt cache is account scoped and stores no receipt PII in browser storage',()=>{const auth={currentUser:{uid:'alice'}};const local=new Map();const session=new Map([['eshwe.latestOrderConfirmation','legacy private receipt']]);const storage=map=>({getItem:key=>map.get(key)||null,setItem:(key,value)=>map.set(key,value),removeItem:key=>map.delete(key)});const api=loadSource('src/lib/order-confirmation.ts',{'@/lib/firebase':{auth}},{window:{},localStorage:storage(local),sessionStorage:storage(session)});const confirmation={userId:'alice',internalOrderId:'order',customer:{address:'Private address'}};api.saveLatestOrderConfirmation(confirmation);assert.equal(api.readLatestOrderConfirmation().customer.address,'Private address');assert.equal(session.size,0);assert.deepEqual([...local.values()],['order']);auth.currentUser={uid:'bob'};assert.equal(api.readLatestOrderConfirmation(),null);api.saveLatestOrderConfirmation(confirmation);assert.equal(api.readLatestOrderConfirmation(),null);api.clearReceiptCache();auth.currentUser={uid:'alice'};assert.equal(api.readLatestOrderConfirmation(),null);});
